{% extends "base.html.twig" %}
{% block body %}


    <!-- Accordion -->
    <h5 class="mt-4">Accordion</h5>
    <div class="row">
        <div class="col-md mb-4 mb-md-0">
            <small class="text-light fw-semibold">Basic Accordion</small>
            <div class="accordion mt-3" id="accordionExample">
                {% for i in 1..5 %}
                    {% set open = i==1 %}
                    <div class="card accordion-item {{ open ? 'active' }}">
                        {# as attributes #}
                        <twig:accordion :open="open"
                                        :header="company_jobTitle()"
                                        :body="lorem_sentences(40, true)"
                        >
                        </twig:accordion>

                        {# as blocks #}
{#                        <twig:accordion :open="open">#}
{#                            <twig:block name="header">#}
{#                                {{ company_jobTitle() }}#}
{#                            </twig:block>#}
{#                            <twig:block name="body">#}
{#                                {{ lorem_sentences(40, true) }}#}
{#                            </twig:block>#}
{#                        </twig:accordion>#}

                    </div>
                {% endfor %}
            </div>
        </div>

    </div>

{% endblock body %}
